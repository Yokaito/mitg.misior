// @ts-check

/**
 * @type {import('next').NextConfig}
 **/
const nextConfig = {
  /* config options here */
  reactStrictMode: false,
  optimizeFonts: true,
  compiler: {
    styledComponents: true,
  },
};

module.exports = nextConfig;
